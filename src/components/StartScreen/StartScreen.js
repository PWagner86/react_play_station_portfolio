import './StartScreen.css';

export const StartScreen = ( { startScreen }) => {
  return <div className={ `start-screen flex-center ${ startScreen }` }>

      <h1>Welcome to my Page</h1>

  </div>;
};
